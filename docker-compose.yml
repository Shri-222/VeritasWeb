services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: veritasweb:local
    env_file:
      - .env.docker
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
    ports:
      - "3000:3000"
    init: true
